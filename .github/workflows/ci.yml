name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Least-privilege default — limits blast radius if a step is compromised.
# Jobs needing write access declare it explicitly at the job level.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  copy-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Check for em dashes in user-facing content
        run: |
          set -e
          FAIL=0
          # Matches literal U+2014 em dash and the CSS \2014 escape (with optional leading zeros).
          PATTERN='—|\\0*2014'
          if grep -rnE "$PATTERN" src/ --include='*.tsx' --include='*.ts' --include='*.css' 2>/dev/null; then
            FAIL=1
          fi
          for f in index.html README.md; do
            if [ -f "$f" ] && grep -HnE "$PATTERN" "$f" 2>/dev/null; then
              FAIL=1
            fi
          done
          if [ "$FAIL" -ne 0 ]; then
            echo ""
            echo "Em dashes found in user-facing files."
            echo "Replace with period, comma, or colon (or \\00b7 middle dot in CSS). See copy-rules.md rule #19."
            exit 1
          fi
          echo "No em dashes found."

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: 20
          cache: npm

      - run: npm ci --ignore-scripts
      - run: npm run build

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: static-site
          path: dist/
          retention-days: 7

  lighthouse:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: static-site
          path: dist/

      - name: Serve static site
        run: |
          npx serve dist -l 8080 &
          sleep 3

      - name: Run Lighthouse
        uses: treosh/lighthouse-ci-action@3e7e23fb74242897f95c0ba9cabad3d0227b9b18 # v12
        with:
          urls: http://localhost:8080/
          uploadArtifacts: true
          temporaryPublicStorage: true
          configPath: .github/lighthouse/lighthouserc.json

      - name: Format Lighthouse results
        if: always()
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8.0.0
        with:
          script: |
            const fs = require('fs');
            const globber = await glob.create('.lighthouseci/lhr-*.json');
            const files = await globber.glob();
            if (files.length === 0) return;
            const lhr = JSON.parse(fs.readFileSync(files[0], 'utf8'));
            const scores = Object.entries(lhr.categories).map(([key, cat]) => {
              const score = Math.round(cat.score * 100);
              const emoji = score >= 90 ? '🟢' : score >= 50 ? '🟡' : '🔴';
              return `| ${cat.title} | ${emoji} ${score} |`;
            });
            const body = [
              '## Lighthouse Results',
              '',
              '| Category | Score |',
              '|----------|-------|',
              ...scores,
              '',
              `> Tested: ${lhr.finalUrl}`,
            ].join('\n');

            // Enforce minimum scores — fail CI if any category is below threshold
            const thresholds = {
              performance: 90,
              accessibility: 90,
              'best-practices': 85,
              seo: 90,
            };
            const failures = [];
            for (const [key, cat] of Object.entries(lhr.categories)) {
              const score = Math.round(cat.score * 100);
              const min = thresholds[key];
              if (min && score < min) {
                failures.push(`${cat.title}: ${score} (minimum: ${min})`);
              }
            }

            const statusLine = failures.length > 0
              ? `\n\n**CI will fail** — scores below threshold:\n${failures.map(f => `- ${f}`).join('\n')}`
              : '\n\nAll scores meet minimum thresholds.';

            const fullBody = body + statusLine;

            // Write to job summary
            await core.summary.addRaw(fullBody).write();

            // Comment on PR if this is a pull request
            if (context.payload.pull_request) {
              const marker = '<!-- lighthouse-ci -->';
              const { data: comments } = await github.rest.issues.listComments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
              });
              const existing = comments.find(c => c.body.includes(marker));
              const commentBody = `${marker}\n${fullBody}`;
              if (existing) {
                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: existing.id,
                  body: commentBody,
                });
              } else {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.payload.pull_request.number,
                  body: commentBody,
                });
              }
            }

            // Fail the step if any score is below threshold
            if (failures.length > 0) {
              core.setFailed(`Lighthouse scores below threshold:\n${failures.join('\n')}`);
            }
