# Set to "true" to show live GitHub star count in the nav badge.
# Off by default so CI, bots, and local dev don't burn unauthenticated API quota.
# Set in Vercel project env vars for production.
VITE_GITHUB_STARS=true
